{
  "name": "miu",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "build": "concurrently \"cd frontend && npm run build\" \"cd backend && npm run build\"",
    "start": "concurrently \"cd frontend && npm run start\" \"cd backend && npm run start\"",
    "lint": "concurrently \"cd frontend && npm run lint\" \"cd backend && npm run lint\"",
    "desktop:dev": "cd nodejs-client && npm run dev",
    "desktop:build": "cd nodejs-client && npm run build",
    "desktop:pack": "cd nodejs-client && npm run pack"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "concurrently": "^9.1.2",
    "dotenv": "^16.4.7",
    "eslint": "^9.21.0",
    "typescript": "^5.0.0"
  }
}
