# MIU GTK Player Requirements
# Python dependencies for the GTK4 + Libadwaita music player

# Core PyGObject bindings
PyGObject>=3.42.0

# GStreamer Python bindings (for audio playback)
# Note: System packages are preferred:
# Ubuntu/Debian: python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1 gir1.2-gst-plugins-base-1.0 gir1.2-gstreamer-1.0
# Arch: python-gobject gtk4 libadwaita gstreamer gst-plugins-base gst-plugins-good
# Fedora: python3-gobject gtk4-devel libadwaita-devel gstreamer1-devel

# Optional: Desktop notifications
# System package: gir1.2-notify-0.7 (Ubuntu/Debian)

# Note: These are system-level dependencies that should be installed via package manager
# The actual Python packages are provided by the system's PyGObject installation